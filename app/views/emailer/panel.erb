Hi <%= @to_employee.name %>,
  You have been added to the interview panel for the recuitment of
  '<%= @resume.name %>' on date
  <% if @interview.interview_date %>
    <%= @interview.interview_date %>
  <% else %>
    ( No date given )
  <% end %> at time
  <%= @interview.interview_time.to_s[11..18] %> for requirement (<%= @requirement.name %>).

  For more details, please login to your recruitment account.
  <% url = url_for(:protocol => "https", :host => APP_CONFIG['host_name'], :controller => "resumes", :action => "show", :id => @uniqid.name) %>
  Link to resume:-  <%= url %>
